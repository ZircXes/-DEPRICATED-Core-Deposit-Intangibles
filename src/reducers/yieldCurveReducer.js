import * as types from '../actions/actionTypes';
import initialState from './initialState';

export default function yieldCurveReducer(state = initialState.yieldCurve, action) {
  switch (action.type) {

    default:
      return state;
  }
}

